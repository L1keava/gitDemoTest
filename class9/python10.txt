1、P86课后习题1，2,3
2、应用read（size）方法读取硬盘中的任意文件
3、应用read()和readlines（）方法读取文件的全部内容
4、将“我爱中国"写入到任意的文件中，并读取
5、P93  tell()  seek()-偏移txt文件，以二进制偏移中间位置
6、已知学生信息存放到stu.txt中，编程实现：
（1）读取全部信息，并输出
（2）按成绩排序，然后写入到px.txt中，并读取内容输出
（3）求成绩的平均值，最大值，最小值
7、写一个 Python 程序，它将读取file.txt并创建一个新文件，其中包含原始文件的内容，但每个单词后都添加一个空格，输出结果。